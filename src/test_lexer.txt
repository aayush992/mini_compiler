// Test file for enhanced lexer
/* This is a multi-line
   comment test */

// Variable declarations
int x = 42;
float y = 3.14;
string message = "Hello, world!\n";
bool isValid = true;

// Test operators
if (x >= 0 && y <= 10.0) {
    // Test string with escape sequences
    string path = "C:\\Program Files\\Test";
    
    if (isValid != false) {
        x = x + 1;
    }
}

// Test all operators
int a = 5;
int b = 10;
bool result = (a == b) || (a != b) && !(a < b); 